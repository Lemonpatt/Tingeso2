server:
  port: 8094
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ms5-db
    username: postgres
    password: 123
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/