server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ms3-db
    username: postgres
    password: 123
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/