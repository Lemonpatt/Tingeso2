server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ms1-db
    username: postgres
    password: 123
eureka:
  instance:
    hostname: localhost
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka/